package main

import "fmt"

func main() {
	// penniesPerText := 2
	// %T prints the type of the variable rather than its values
	// In this case it will print "The type of penniesPerText is int"
	// fmt.Printf("The type of penniesPerText is %T\n", penniesPerText)
	// However if we wish to change it to a float 64
	penniesPerText := 2.0
	fmt.Printf("The type of penniesPerText is %T\n", penniesPerText)
}
